piepline {
    agent any 
    stages {
        stage('STAGE-1') {
            when {
                expression { env.BRANCH_NAME != 'master'}
            }
            steps{
                sh "echo STEGE-2 executes if branch is master"
            }
        }

        stage('STAGE-2'){
            when {
                expression { env.BRANCH_NAME != 'master'}
            }
            steps{
                sh "echo STEGE-2 executes if branch is master"
            }
        }
    }
}